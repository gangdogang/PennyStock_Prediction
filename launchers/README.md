# Launchers

- `windows/`: Windows `.bat` / `.ps1` launchers and Task Scheduler helpers live here.
- Repository root: macOS `.command` launchers stay at the top level so they are easy to double-click in Finder.

If you are using Windows, start in `/Users/wondokyeong/Desktop/Penny_Stock/launchers/windows`.
