# 2026-04-21 설계 리뷰 모음

이 문서는 4개 창에서 나온 설계 리뷰를 모으기 위한 파일이다.

아래 내용은 **1번 리뷰**다. 나머지 2번, 3번, 4번 리뷰는 이 문서 뒤에 이어서 덧붙인다.

---

## 1번 리뷰 - Quant Research 설계 리뷰

역할: Quant researcher

목표: 페니주 predictor/watchlist/momentum 전략의 edge를 통계적으로 검증하는 실험 설계

읽은 기준 문서:

- `README.md`
- `docs/STATUS.md`
- `docs/BACKTEST_ROADMAP_KO.md`
- `docs/STEP_PROGRESS_KO.md`

### 1. 지금 결과로 edge 판단이 어려운 이유

현재 paper/backtest 결과는 "전략이 돈을 벌었나"보다 "측정 배선이 제대로 되었나" 단계에 가깝다.

- 과거 CSV는 `predictor_score`, `predictor_weight`, KPI 분모가 불완전할 수 있어 predictor 기여도를 신뢰하기 어렵다.
- 과거 관찰에서 `predictor_weighted`와 `momentum_only` 거래/손익이 동일했던 적이 있어 bucket divergence가 실제 alpha 차이를 반영했는지 불명확하다.
- Step 0 데이터 조건, 특히 point-in-time universe, KIS minute/L1 coverage, halt/LULD, historical spread가 아직 충분하지 않다. 페니주는 survivorship, spread, halt, partial fill 하나가 결과를 크게 왜곡한다.
- 1개월 결과는 버킷당 대략 수십 거래 수준이라 regime 하나와 outlier 한두 건에 지배된다.
- tuning set과 validation set이 분리되지 않으면 predictor threshold, sizing, momentum threshold가 결과에 맞춰진 것인지 실제 edge인지 구분할 수 없다.
- 현재 리포트가 있어도 Step 0 coverage와 shadow/out-of-sample 전에는 live 판단 근거가 될 수 없다는 문서 상태와 일치한다.

### 2. Bucket Taxonomy

alpha를 분리하려면 "선별 alpha", "예측 alpha", "실시간 momentum alpha"를 factorial하게 쪼개야 한다.

| Bucket | 목적 | 선택 | 실행 |
| --- | --- | --- | --- |
| `control_fixed` | 순수 기준선 | point-in-time universe에서 가격/float/volume/sector/regime 매칭 control | 고정 진입/고정 청산 |
| `control_momentum` | momentum 자체의 일반 alpha | 같은 matched control | live momentum rule, predictor off |
| `watchlist_fixed` | watchlist alpha | watchlist top-N | 고정 진입/고정 청산 |
| `watchlist_momentum` | watchlist 조건부 momentum alpha | watchlist top-N | live momentum rule, predictor off |
| `predictor_fixed` | predictor alpha | watchlist 안에서 predictor top/weighted | 고정 진입/고정 청산 |
| `predictor_weighted_momentum` | 실제 조합 전략 | watchlist + predictor weight | live momentum rule + predictor weight |

효과 추정:

- Watchlist alpha: `watchlist_fixed - control_fixed`
- Momentum alpha: `watchlist_momentum - watchlist_fixed`, 보조로 `control_momentum - control_fixed`
- Predictor alpha: `predictor_fixed - watchlist_fixed`, 보조로 `predictor_weighted_momentum - watchlist_momentum`
- Interaction alpha: `predictor_weighted_momentum - predictor_fixed - watchlist_momentum + watchlist_fixed`

기존 `predictor_weighted` vs `momentum_only`는 유지하되, 그 차이만 보면 predictor alpha와 watchlist/momentum interaction이 섞인다.

### 3. Information Invariants

| Bucket | 허용 정보 | 금지 정보 |
| --- | --- | --- |
| 모든 bucket | D일 기준 point-in-time universe, D 시점까지 존재한 상장/가격/분할 정보, 실행 시점 이전 quote/bar | 현재 universe로 과거 재생, D 이후 상폐/합병을 알고 제외, 미래 split 보정 오류 |
| `control_*` | cutoff 이전 가격, float, volume, sector 등 matching 변수 | SEC/watchlist 점수, predictor score, 정규장 이후 성과 |
| `watchlist_*` | `filed_at <= D 08:00 ET` 공시, cutoff 이전 premarket/watchlist 입력 | cutoff 이후 공시, 장중 high/low/close, EOD regime |
| `predictor_*` | watchlist 입력, cutoff 이전 premarket features, 저장된 `prediction_generated_at`, `prediction_cutoff_at` | 장중 trigger 여부, realized PnL, day high/low, 사후 theme labeling |
| `*_momentum` | decision timestamp 이전 live quote/activity, stale/halt/spread guard | 다음 bar high/low, 당일 종가, fill 이후 quote를 entry 판단에 사용 |
| 모든 비교 | 동일 초기자본, 비용, slippage model, coverage rule, seed, settings hash | bucket별 유리한 fill model, missing data를 성과 좋은 쪽만 포함 |

핵심 invariant는 "선택 시점에 볼 수 없던 정보는 outcome 계산 외에는 절대 쓰지 않는다"이다.

### 4. 샘플/기간/검증 기준

최소 기준:

- 1개월 sanity: 20 거래일 내외, bucket당 최소 30-50 closed trades. edge 판단 금지.
- 3개월 validation: 60-65 거래일, 주요 bucket당 최소 150 closed trades 권장, 최소 100 미만이면 통계 판단 보류.
- Predictor score bucket: decile은 샘플 부족 가능성이 커서 초기에는 quintile 또는 tertile. 각 bucket 최소 30 trades 또는 30 candidates.
- Active trading day: 최소 40일 이상. 거래가 특정 5-10일에 몰리면 독립 표본으로 보지 않는다.

Walk-forward:

- 이상적: 3개월 train -> 1개월 validation rolling, 최소 6 folds.
- 현실적 초기안: 1개월 calibration으로 파라미터 후보 축소 -> 이후 3개월 out-of-sample 단 1회 실행.
- validation 결과를 보고 threshold를 바꾸면 그 기간은 다시 train으로 강등한다.

Bootstrap / Monte Carlo:

- primary: paired day-level bootstrap 10,000회. 같은 날짜의 bucket delta를 같이 resampling해 시장 regime correlation을 보존한다.
- secondary: trade-level bootstrap은 day cluster를 유지하거나 symbol/day block으로 resampling한다.
- MDD/risk: trade-order Monte Carlo 10,000회. 단, 완전 무작위 shuffle보다 day block 순서 재배열이 더 보수적이다.
- pass 기준 예: net R expectancy delta의 95% CI 하단이 0 초과, profit factor CI 하단이 1 초과 또는 최소 허용값 초과, one-winner dependency 제거 후에도 양수.

### 5. KPI 우선순위

핵심 KPI:

- Net expectancy in R: 수수료/슬리피지 차감 후 거래당 기대값.
- Paired excess expectancy: bucket A-B의 day-paired delta.
- Profit factor with CI.
- Max drawdown / Monte Carlo drawdown CI.
- Cost-adjusted return.
- One-winner dependency 제거 후 PnL.
- Predictor 전용: score quintile별 monotonic lift, top-bottom spread, predictor IC/Spearman, edge decay day1/day2/day3.
- Execution 전용: stop execution slippage, spread cost, partial fill rate.

보조 KPI:

- Raw win rate.
- Raw total return.
- Sharpe. 거래 수가 적으면 불안정하므로 CI와 함께만 본다.
- Avg hold time.
- MFE/MAE histogram.
- Theme concentration.
- Time-under-water.
- Candidate survival rate. predictor funnel 진단에는 중요하지만 단독 edge 증거는 아니다.

### 6. 1개월 Sanity -> 3개월 Validation -> Shadow 연결

1개월 sanity:

- 목적은 성과가 아니라 배선 검증이다.
- 확인 항목: lineage 공백 없음, bucket divergence 존재, 비용 반영, fill/slippage 현실성, `candidate_count` 분모 일관성, outlier 의존도.
- 이 구간에서 threshold, `k1`, `k2`, sizing 후보를 좁힌다.

3개월 validation:

- 1개월 sanity에서 고른 설정을 freeze한다.
- 이후 3개월 replay를 out-of-sample로 단 1회 실행한다.
- `control_fixed`, `watchlist_fixed`, `watchlist_momentum`, `predictor_fixed`, `predictor_weighted_momentum`을 모두 같은 기간/비용/coverage로 비교한다.
- 결과는 paired bootstrap CI와 one-winner 제거 결과까지 같이 본다.

Shadow:

- 최소 2-4주, 가능하면 20 거래일 이상.
- 실주문 없이 매일 predictor/watchlist/momentum decision을 저장한다.
- shadow에서는 tuning 금지다.
- 비교 대상은 PnL만이 아니라 trigger rate, predictor top-quintile lift, edge decay, slippage estimate, stale/halt reject rate다.
- shadow KPI가 3개월 replay CI 바깥으로 크게 벗어나면 데이터 리크, replay/live feature mismatch, execution model 오류를 의심하고 live 전환을 보류한다.

### 7. 이상적 설계 vs 지금 가능한 설계

이상적 설계:

- 12-24개월 point-in-time universe.
- delisted/split/corporate action 완전 보정.
- full L1 또는 tape 기반 bid/ask, halt/LULD 이벤트.
- 3m train -> 1m validation rolling walk-forward 6회 이상.
- matched control universe와 naive premarket gainer baseline 동시 운영.
- live shadow 1개월 이상 후 tiny-size 단계 진입.

지금 당장 가능한 설계:

- Step -1 배선이 완료된 기준으로 새 paper export를 다시 생성한다.
- Step 0 coverage 60% 확보 전까지는 "edge 검증"이 아니라 "coverage-aware sanity"로 표시한다.
- 기존 두 bucket에 더해 최소 `watchlist_fixed`, `predictor_fixed`, `control_fixed` 개념을 리포트 설계에 추가해 alpha 분해 기준을 먼저 고정한다.
- KIS minute/L1이 있는 날짜부터 5-10일 sanity, 1개월 calibration을 돌린다.
- 3개월 replay는 coverage가 충분한 날짜 집합으로 구성하되, coverage filter가 성과를 유리하게 고르지 않았는지 별도 리포트가 필요하다.
- shadow predictor 저장은 지금 시작해도 된다. 단, 그 결과로 파라미터를 계속 바꾸면 shadow가 아니라 forward tuning이 된다.

---

## 2번 리뷰

역할: Skeptical reviewer / red team

목표: 전략 평가가 과대평가되거나 잘못된 결론을 낼 수 있는 경로를 찾는다.

읽은 기준 문서:

- `README.md`
- `docs/STATUS.md`
- `docs/BACKTEST_ROADMAP_KO.md`
- `docs/STEP_PROGRESS_KO.md`

### 총평

Step -1 배선은 상당 부분 고정됐지만, Step 0 데이터/재현성/실행 괴리 gate가 아직 hard gate가 아니다. 지금 결과를 "전략 edge"로 해석하면 과대평가 경로가 많다. 특히 `latest` fallback, live screener 유입, 최신 prediction fallback, coverage non-strict 처리가 핵심 리스크다.

### 1. survivorship bias

- 현재 위험도: High
- 근거:
  - point-in-time 경로는 있지만 기본/대체 경로가 최신 universe로 떨어진다.
  - `src/penny_stock_radar/services/backtest_data.py`
  - `src/penny_stock_radar/cli/backtest.py`
  - `src/penny_stock_radar/services/kis_historical.py`
- 막을 gate/test/manifest:
  - backtest 성능 리포트는 point-in-time universe 없으면 fail.
  - `run_manifest.json`에 `point_in_time_scan_id`, universe count, current-vs-PIT diff 필수.
- 지금 당장 막을 것:
  - PIT universe 누락 시 성능평가 금지.
  - latest universe fallback을 backtest 경로에서 fail 처리.
- 나중에 막을 것:
  - delist/reverse split/merger corporate action manifest.

### 2. look-ahead bias

- 현재 위험도: Medium
- 근거:
  - SEC cutoff 테스트는 있지만 `run-premkt-predictor` 기본 CLI는 `market_date`/cutoff 없이 최신 기준으로 실행된다.
  - `src/penny_stock_radar/services/watchlist_builder.py`
  - `src/penny_stock_radar/services/filing_scanner.py`
  - `src/penny_stock_radar/cli/backtest.py`
- 막을 gate/test/manifest:
  - replay/backtest 모드에서는 `market_date`와 `prediction_cutoff_at` 없으면 fail.
  - filing fixture에서 cutoff 이후 공시가 watchlist/predictor에 들어오면 fail.
- 지금 당장 막을 것:
  - historical predictor 실행에는 명시 cutoff 강제.
- 나중에 막을 것:
  - 모든 feature row에 `as_of_at` 컬럼/manifest.

### 3. universe leakage

- 현재 위험도: High
- 근거:
  - backtest용 watchlist라도 live screener/recent market activity/yfinance metadata가 dynamic rows로 들어갈 수 있다.
  - `src/penny_stock_radar/services/watchlist_builder.py`
- 막을 gate/test/manifest:
  - historical replay에서는 live context와 dynamic rows 금지.
  - universe source manifest를 fail 조건으로 검사.
- 지금 당장 막을 것:
  - `market_date`가 있는 watchlist/predictor build에서 `_build_market_context()` 비활성화.
- 나중에 막을 것:
  - metadata도 PIT snapshot으로 저장.

### 4. watchlist/predictor contamination

- 현재 위험도: High
- 근거:
  - predictor가 `WatchlistBuilder`를 호출하며 live/recent/social context를 함께 먹을 수 있다.
  - reporting은 최신 premkt prediction을 symbol 기준 fallback으로 붙인다.
  - `src/penny_stock_radar/services/premkt_predictor.py`
  - `src/penny_stock_radar/services/watchlist_builder.py`
  - `src/penny_stock_radar/services/paper_reporting.py`
- 막을 gate/test/manifest:
  - prediction lookup은 `run_id/scan_id/market_date/cutoff`로만 join.
  - 최신 prediction fallback 금지.
- 지금 당장 막을 것:
  - `fetch_latest_premkt_predictions()` 기반 fallback을 성능평가 경로에서 fail.
- 나중에 막을 것:
  - social/recent activity에도 as-of cutoff 도입.

### 5. live scan vs replay 불일치

- 현재 위험도: High
- 근거:
  - mock replay는 최신 watchlist/universe에서 synthetic scenario를 생성한다.
  - live paper는 provider top gainers/actives를 사용한다.
  - 같은 입력 로그 재생이 아니다.
  - `src/penny_stock_radar/services/replay_pipeline.py`
  - `src/penny_stock_radar/services/market_activity.py`
- 막을 gate/test/manifest:
  - 성능 리포트 manifest에 data source가 `synthetic/live/replay_from_archive`인지 필수 표기.
  - synthetic은 edge 판단 금지.
- 지금 당장 막을 것:
  - mock replay 결과에 `sanity_only=true` manifest.
- 나중에 막을 것:
  - live `MarketActivity` JSONL을 그대로 replay하는 adapter.

### 6. stale quote / halt / LULD

- 현재 위험도: High
- 근거:
  - stale/live quote guard는 있지만 KIS market status는 live field 존재 시 사실상 `open`으로 추정한다.
  - 공식 halt/LULD feed가 아니다.
  - `src/penny_stock_radar/services/paper_trading.py`
  - `src/penny_stock_radar/services/trading_support.py`
  - `src/penny_stock_radar/providers/live_market.py`
- 막을 gate/test/manifest:
  - halt/LULD source 부재 시 `halt_coverage_missing` gate.
  - stale/halt 발생일은 성능 판단 제외 또는 별도 bucket.
- 지금 당장 막을 것:
  - halt/LULD coverage 없는 replay를 edge 평가에서 fail.
- 나중에 막을 것:
  - official halt feed 및 resume auction handling.

### 7. slippage / spread / partial fill 모델

- 현재 위험도: Medium
- 근거:
  - 모델은 있으나 buy fill은 ask를 그대로 체결가로 쓰고 spread multiplier는 주로 report metric이다.
  - partial fill 잔량은 다음 봉으로 이월되지 않는다.
  - `src/penny_stock_radar/services/fill_model.py`
  - `src/penny_stock_radar/services/engine_rules/entry.py`
- 막을 gate/test/manifest:
  - L1 coverage 미달이면 fill realism gate fail.
  - partial 잔량 drop 여부를 manifest warning이 아니라 fail.
- 지금 당장 막을 것:
  - partial fill 발생 시 remaining quantity 처리 정책을 명시하고 검증.
- 나중에 막을 것:
  - queue/depth/latency 기반 체결 모델.

### 8. KPI denominator 문제

- 현재 위험도: Medium
- 근거:
  - Step -1 gate는 생겼지만 `candidate_count`는 최신 prediction lookup 전체 길이이고 run/date/cutoff scoped가 아니다.
  - `src/penny_stock_radar/services/paper_reporting.py`
- 막을 gate/test/manifest:
  - KPI denominator는 manifest의 exact prediction snapshot id에서만 계산.
  - `candidate_count`, `triggered_candidate_count`, `predicted_trade_count` 모두 run-scoped 검사.
- 지금 당장 막을 것:
  - prediction snapshot id가 없으면 predictor KPI fail.
- 나중에 막을 것:
  - open trade 포함/제외 KPI 분리.

### 9. repeated tuning / overfitting

- 현재 위험도: High
- 근거:
  - 문서는 out-of-sample 분리를 요구하지만 코드 manifest에는 tuning iteration, split id, freeze 여부가 없다.
  - `docs/BACKTEST_ROADMAP_KO.md`
  - `src/penny_stock_radar/services/paper_reporting.py`
- 막을 gate/test/manifest:
  - `run_manifest.json`에 `dataset_role=smoke|calibration|out_of_sample`, `parameter_freeze_id`, `tuning_run_id` 필수.
- 지금 당장 막을 것:
  - OOS라고 부르는 run에는 freeze id 없으면 fail.
- 나중에 막을 것:
  - walk-forward registry와 재사용된 OOS 구간 fail.

### 10. sample size와 regime 편향

- 현재 위험도: High
- 근거:
  - 문서가 1개월은 검증이 아니라고 명시한다.
  - 현재 Step 0 coverage도 미완이다.
  - `docs/STATUS.md`
  - `docs/BACKTEST_ROADMAP_KO.md`
- 막을 gate/test/manifest:
  - `closed_trade_count`, 거래일 수, regime count가 최소치 미만이면 performance gate fail.
- 지금 당장 막을 것:
  - 최소 표본/최소 거래일/최소 regime 조건 전까지 모든 KPI를 `sanity_only`로 라벨링.
- 나중에 막을 것:
  - rolling 3개월+ OOS와 regime별 최소 표본 조건.

### 11. baseline이 너무 약하거나 너무 강한 문제

- 현재 위험도: Medium
- 근거:
  - baseline은 pct/volume leader 2개뿐이고 cash/SPY/no-trade가 없다.
  - excess Sharpe도 `baseline_pct` 하나만 기준이다.
  - `src/penny_stock_radar/services/paper_coordinator.py`
  - `src/penny_stock_radar/services/engine_rules/entry.py`
  - `src/penny_stock_radar/services/paper_reporting.py`
- 막을 gate/test/manifest:
  - baseline 부재/closed trade 0이면 비교 KPI fail.
  - cash/no-trade baseline은 즉시 추가 대상.
- 지금 당장 막을 것:
  - 최소 baseline 세트를 manifest에 명시하고 누락 시 fail.
- 나중에 막을 것:
  - SPY/IWM, top-gainer buy-at-open, random matched-liquidity baseline.

### 12. paper trading과 실제 체결 사이 괴리

- 현재 위험도: High
- 근거:
  - 문서상 shadow/실제 체결 검증이 아직 gap이다.
  - paper engine은 즉시 cash/position mutation이다.
  - `docs/LIVE_TRADING_READINESS_PLAN_KO.md`
  - `src/penny_stock_radar/services/engine_rules/entry.py`
- 막을 gate/test/manifest:
  - paper 성과는 live 전환 근거로 사용 금지.
  - `shadow_days >= 10`, reject/slippage/reconciliation manifest 없으면 live readiness fail.
- 지금 당장 막을 것:
  - paper report에 `not_live_ready=true` 또는 동등한 상태 명시.
- 나중에 막을 것:
  - broker fill reconciliation, reject telemetry, restart recovery runbook.

### 가장 먼저 막을 gate

1. PIT universe + prediction cutoff + data source manifest 없으면 성능 리포트 fail.
2. Step 0 L1 coverage gate를 non-strict warning에서 strict fail로 전환. 현재 기본 quality gate는 실패/누락도 통과한다.
3. latest prediction/universe fallback을 backtest 경로에서 금지.
4. 최소 표본/최소 거래일/최소 regime 조건 전까지 모든 KPI를 `sanity_only`로 라벨링.

---

## 3번 리뷰 - Trading Systems Engineer 설계 리뷰

역할: Trading systems engineer

목표: 현재 paper/backtest/live scan 구조를 보고, predictor/watchlist/momentum alpha를 분리 측정할 수 있는 엔진 구조를 설계한다.

읽은 기준 문서:

- `README.md`
- `docs/STATUS.md`
- `docs/BACKTEST_ROADMAP_KO.md`
- `docs/STEP_PROGRESS_KO.md`

### 결론

현재 `momentum_only`는 pure momentum이 아니라 **watchlist-aware momentum with predictor weight disabled**다.

따라서 `predictor_weighted`와 `momentum_only`가 같은 종목을 사고 `pnl_diff=0`이 나는 것은 단순 버그라기보다, 현재 bucket 정의가 predictor/watchlist/momentum alpha를 분해하기에 부족하다는 신호다.

### 1. Bucket contamination 지점

#### 1.1 Scanner 단계에서 watchlist가 activity에 섞임

- `src/penny_stock_radar/services/market_activity.py`
- `MarketActivityScanner.scan()`이 `fetch_latest_watchlist()`를 읽는다.
- `_snapshot_to_activity()`에서 `watchlist_rank is not None`이면 `predicted=True`가 된다.

결과적으로 engine에 들어가는 `MarketActivity`는 이미 watchlist context를 포함한다.

#### 1.2 Scanner analysis score 자체가 watchlist-aware

- `src/penny_stock_radar/services/market_activity.py`
- `_analysis()`에서 `predicted`면 score에 `+1.0`, `watchlist_score >= 3.0`이면 `+0.5`가 더해진다.
- `OPENING_RANGE_CANDIDATE`, `CONDITIONAL_ENTRY` 같은 label도 `has_context`에 영향을 받는다.

따라서 나중에 `predicted=False`나 `watchlist_rank=None`으로 후처리해도 이미 `analysis_score`와 `analysis_label`이 오염돼 있다.

#### 1.3 Coordinator가 같은 activity를 모든 bucket에 공유

- `src/penny_stock_radar/services/paper_coordinator.py`
- primary `predictor_weighted`와 secondary `momentum_only`가 같은 `activity` list를 처리한다.

bucket별 정보 격리가 policy 레벨에서 강제되지 않는다.

#### 1.4 `momentum_only`가 끄는 것은 predictor score/weight뿐

- `src/penny_stock_radar/services/paper_trading.py`
- `src/penny_stock_radar/services/engine_rules/entry.py`
- `momentum_only`일 때 `_predictor_score_by_symbol`은 비워지고 `predictor_weight=0.0`이 된다.

하지만 아래 정보는 그대로 남는다.

- `row.predicted`
- `row.watchlist_rank`
- `row.watchlist_score`
- `watchlist_predicted`, `watchlist_score_strong` reasons
- watchlist context가 반영된 `analysis_label`
- watchlist context가 반영된 `analysis_score`

#### 1.5 Adaptive entry rule도 watchlist를 직접 사용

- `eligible_for_predicted_priority_entry()`는 `row.predicted`를 요구한다.
- `adaptive_entry_sort_key()`는 `predicted_starter`에서 `watchlist_rank`를 우선 정렬한다.
- `entry_fraction()`은 watchlist rank에 따라 sizing base fraction을 바꾼다.

따라서 predictor weight를 꺼도 watchlist alpha와 watchlist-conditioned sizing은 남는다.

#### 1.6 Predictor 자체도 watchlist에서 파생

- `src/penny_stock_radar/services/premkt_predictor.py`
- `PremktPredictor.run()`은 `WatchlistBuilder.build()` 결과를 입력으로 prediction을 만든다.

즉 predictor alpha와 watchlist alpha는 입력 단계부터 독립이 아니다.

#### 1.7 Replay mock도 watchlist 중심

- `src/penny_stock_radar/services/replay_pipeline.py`
- `generate_mock_replay()`는 latest watchlist row만으로 mock replay spec을 만든다.

이 경로만으로는 pure momentum이나 outside-watchlist missed winner를 측정하기 어렵다.

### 2. 엔진 분리 설계

권장 구조:

```text
BucketPolicy
  -> UniverseProvider
  -> Raw market snapshot / historical tape
  -> ActivityBuilder
  -> ActivitySanitizer 또는 policy-aware analysis
  -> ReplayRunner / PaperCoordinator
  -> Reporting
```

#### 2.1 BucketPolicy

새 policy object가 bucket별 허용 정보를 명시해야 한다.

필드 예:

- `bucket`
- `label`
- `uses_predictor_weight`
- `uses_predictor_lineage`
- `uses_watchlist_context`
- `uses_watchlist_universe`
- `uses_screener_universe`
- `recompute_analysis`
- `legacy_bucket_alias`

bucket string 비교를 entry rule 내부에 흩뿌리지 말고, policy capability로 판단하는 구조가 낫다.

#### 2.2 ActivitySanitizer

bucket별 activity를 분리 생성한다.

중요한 점은 단순 field 제거가 충분하지 않다는 것이다. `analysis_score`와 `analysis_label`이 watchlist context 반영 후 생성되므로, blind bucket은 raw snapshot 기준으로 analysis를 다시 계산해야 한다.

역할:

- predictor field 제거
- watchlist rank/score/predicted 제거
- watchlist reasons 제거
- 필요 시 `analysis_score` / `analysis_label` 재계산
- lineage에는 어떤 정보가 제거됐는지 기록

#### 2.3 UniverseProvider

symbol set 책임을 scanner에서 분리한다.

provider 종류:

- `WatchlistUniverseProvider`
- `WatchlistPlusPassedUniverseProvider`
- `LiveScreenerUniverseProvider`
- `PointInTimeHistoricalUniverseProvider`
- `ActivePositionsOverlayProvider`

현재 scanner는 provider screener가 없으면 watchlist를 먼저 쓰는 fallback이라 pure momentum universe를 보장하지 못한다.

#### 2.4 ReplayRunner

동일 raw market data를 bucket별 policy로 재생한다.

원칙:

- raw historical minute/L1 stream은 모든 bucket에 동일
- 각 bucket은 자기 policy에 맞는 universe/activity만 받음
- order/fill/cost/slippage model은 동일
- settings hash, universe source, cutoff, policy id를 manifest에 기록

### 3. Bucket 구현 단계

#### v1에서 바로 구현

1. `predictor_weighted`
   - predictor score/weight 사용
   - watchlist context 사용
   - 현재 primary 의미 유지

2. `watchlist_momentum`
   - predictor score/weight 미사용
   - watchlist context 사용
   - 현재 `momentum_only`의 실제 의미

3. `watchlist_blind_momentum`
   - watchlist가 고른 symbol universe는 유지
   - entry/scoring에는 watchlist rank/score/predicted 미사용
   - watchlist selection alpha와 watchlist context alpha를 분리하기 위한 중간 bucket

기존 `momentum_only` key는 DB/CSV/UI 호환 때문에 바로 제거하지 말고, v1에서는 `watchlist_momentum`의 legacy alias로 유지한다.

#### Step 0 coverage 이후 v2

4. `pure_momentum`
   - watchlist universe 미사용
   - watchlist context 미사용
   - live screener 또는 point-in-time historical universe 기준 pct/volume momentum만 사용

이 bucket은 Step 0 historical coverage 없이는 의미가 약하다.

#### v3

- walk-forward / rolling validation
- bucket별 false positive / false negative / missed leader KPI
- live shadow mode에서 4개 policy 동시 평가

### 4. Scanner universe가 watchlist에 묶일 때의 한계

현재 scanner는 provider screener가 있으면 top gainers/most active를 우선 쓰지만, provider screener가 없으면 watchlist rows를 먼저 넣고 passed universe를 뒤에 붙인다.

한계:

- `scan_limit`이 작으면 passed universe가 잘려 outside-watchlist winner가 관찰되지 않는다.
- watchlist에 없는 momentum winner를 missed leader로 측정하기 어렵다.
- watchlist의 selection alpha와 live momentum alpha가 섞인다.
- predictor가 못 고른 종목의 opportunity cost를 과소평가한다.
- pure momentum baseline이 사실상 watchlist-conditioned baseline이 된다.

따라서 pure momentum 평가는 scanner fallback이 아니라 별도 universe provider가 필요하다.

### 5. 파일별 구현 순서

1. `src/penny_stock_radar/services/paper_runtime.py`
   - bucket constants/labels 추가
   - 기존 `momentum_only`는 legacy alias로 유지

2. `src/penny_stock_radar/services/bucket_policy.py`
   - `BucketPolicy` dataclass 추가
   - 기본 4개 policy 정의

3. `src/penny_stock_radar/services/activity_sanitizer.py`
   - policy별 `MarketActivity` 변환
   - watchlist/predictor 정보 제거
   - blind bucket analysis 재계산

4. `src/penny_stock_radar/services/market_activity.py`
   - scanner에서 universe/context policy를 주입할 수 있게 분리
   - 장기적으로 `UniverseProvider` 연결

5. `src/penny_stock_radar/services/paper_coordinator.py`
   - hard-coded 2 bucket 구조를 policy list 기반으로 변경
   - bucket별 sanitized activity 전달

6. `src/penny_stock_radar/services/engine_rules/entry.py`
   - bucket string 비교 대신 policy capability 사용
   - `momentum_only` special case 축소

7. `src/penny_stock_radar/services/paper_reporting.py`
   - 기존 CSV schema 유지
   - 새 bucket row와 `bucket_policy` 같은 append-only 컬럼 추가 가능

8. UI/CLI
   - 기존 명령과 기존 CSV 파일명 유지
   - 새 bucket은 추가 row로 노출

### 6. 테스트 순서

1. `ActivitySanitizer` unit test
   - `watchlist_blind_momentum`에서 `predicted=False`
   - `watchlist_rank=None`
   - `watchlist_score=None`
   - watchlist reasons 제거
   - analysis 재계산 확인

2. entry rule matrix test
   - 같은 raw row에서 4개 bucket이 의도한 entry decision을 내는지 확인

3. coordinator test
   - 각 bucket에 같은 activity 객체가 아니라 policy별 sanitized activity가 들어가는지 확인

4. replay/mock test
   - `watchlist_momentum`과 `watchlist_blind_momentum` 사이 diff 생성

5. reporting compatibility test
   - 기존 CSV 파일명 유지
   - 기존 컬럼 유지
   - 새 bucket row 추가 확인

6. golden regression
   - 기존 `predictor_weighted k0` 유지
   - legacy `momentum_only`가 의도 없이 바뀌지 않는지 확인

### 7. CSV/UI/CLI 호환성

호환성 원칙:

- `momentum_only` bucket key는 바로 제거하지 않는다.
- 기존 CSV 파일명은 유지한다.
- 기존 CSV 컬럼은 삭제/이름 변경하지 않는다.
- 새 정보는 append-only 컬럼 또는 새 CSV로 추가한다.
- UI는 `momentum_only`를 "Watchlist Momentum (legacy momentum_only)"로 표시할 수 있다.
- `paper_bucket_trade_diff.csv`는 기존 predictor vs legacy momentum diff를 유지하고, v2에서 multi-bucket pairwise diff 또는 `paper_alpha_decomposition.csv`를 추가한다.

### 8. 지금 바로 할 v1

- `momentum_only`의 의미를 watchlist-aware momentum으로 명확히 한다.
- `watchlist_momentum` label/policy를 추가하고 `momentum_only`는 alias로 둔다.
- `watchlist_blind_momentum`을 추가한다.
- activity sanitizer를 만든다.
- coordinator가 bucket별 sanitized activity를 전달한다.
- performance gate에 bucket policy / activity contamination warning을 추가한다.

### 9. Step 0 coverage 이후 할 v2

- historical/PIT `UniverseProvider` 추가
- L1/minute archive 기반 replay runner 연결
- `pure_momentum` 추가
- missed leader / outside-watchlist winner 리포트 추가
- `run_manifest.json`에 universe source, policy id, cutoff, coverage를 기록

### 10. v3

- walk-forward / rolling validation
- train/calibration/out-of-sample split registry
- live shadow mode에서 4개 policy 동시 평가
- predictor/watchlist/momentum interaction alpha decomposition report

### 우선순위

가장 먼저 할 일은 `momentum_only`를 pure momentum으로 오해하지 않게 만들고, `watchlist_blind_momentum`을 추가해 watchlist context contamination을 끊는 것이다.

`pure_momentum`은 Step 0 coverage와 PIT universe가 확보된 뒤 구현하는 편이 맞다. 지금 구현하면 이름만 pure momentum이고 실제로는 scanner/provider coverage에 의해 왜곡될 가능성이 높다.

---

## 4번 리뷰 - Pragmatic Tech Lead 실행 로드맵

역할: Pragmatic tech lead

목표: 너무 이상적인 연구 설계가 아니라, 실제로 이 저장소에서 순서대로 구현 가능한 로드맵을 정한다.

이 섹션은 **4개 창 중 4번 창의 리뷰**다. 다른 1번, 2번, 3번 창의 리뷰와 병합할 때 이 번호를 유지한다.

읽은 기준 문서:

- `README.md`
- `docs/STATUS.md`
- `docs/BACKTEST_ROADMAP_KO.md`
- `docs/STEP_PROGRESS_KO.md`

### 결론

다음 우선순위는 `얇은 bucket semantic split 검증 -> Step 0 coverage 적재` 순서가 맞다.

단, bucket 작업은 큰 리네임이나 DB 마이그레이션이 아니다. 지금 필요한 것은 `bucket = 포트폴리오 비교 단위`, `strategy_bucket = 진입/포지션 내부 원인`이라는 계약을 테스트와 리포트 컬럼으로 고정하는 1-2일짜리 작업이다. 그 다음 대부분의 시간은 Step 0 coverage에 써야 한다.

### v1 - 2주 안에 끝낼 범위

목표: 성능 비교가 해석 가능한 상태에서 Step 0 coverage 루프를 실제로 굴린다.

Exit criteria:

- `predictor_weighted` vs `momentum_only`가 포트폴리오 bucket이고, `starter/add_1/predicted_starter/winner_add`는 `strategy_bucket`이라는 의미가 테스트/CSV에서 흔들리지 않는다.
- 최신 paper export에 대해 `review-paper-performance`가 pass한다.
- 최소 2개 market date에 대해 minute/L1 coverage report와 gate status JSON을 생성한다.
- coverage가 60% 미만이어도 무엇이 부족한지가 `automation/state/backtest_coverage/`와 gate status로 설명된다.
- `./scripts/check_quality.sh`가 통과한다.

PR/커밋 단위:

1. `docs: lock v1 execution priority`
   - `docs/STATUS.md` 먼저 갱신: bucket semantic contract 먼저, 그 다음 Step 0 coverage라고 명시.
   - `docs/STEP_PROGRESS_KO.md`에 active tranche 기록.
2. `test: pin paper bucket semantics`
   - `bucket`과 `strategy_bucket` 혼용 방지 테스트.
   - momentum bucket에서 predictor score/weight가 실제로 제거되는지 검증.
   - 리포트에 `portfolio_bucket`/`strategy_bucket`이 같이 나오는지 검증.
3. `ops: run Step 0 coverage smoke`
   - 코드보다 운영 실행 중심.
   - 산출물은 런타임 영역이므로 커밋하지 않는다.
   - 실패/부족분만 문서에 기록한다.
4. `docs: record v1 coverage baseline`
   - coverage 수치, 미달 사유, 다음 적재 날짜/세션 기록.

검증 명령:

```bash
./scripts/check_quality.sh
./scripts/psradar review-paper-performance --export-dir sample_outputs/paper_trading
./scripts/psradar backfill-kis-minute --market-date YYYY-MM-DD --symbol TSLA --symbol SOUN
./scripts/psradar capture-kis-l1-window --symbol TSLA --symbol SOUN --iterations 60 --interval-seconds 60
./scripts/psradar report-backtest-coverage --market-date YYYY-MM-DD --session premarket --symbol TSLA --symbol SOUN
python3 -m penny_stock_radar.quality_gates
```

### v2 - 1개월 범위

목표: Step 0을 실험 가능한 수준으로 올리고, 5-10일 sanity replay와 1개월 calibration 준비까지 간다.

Exit criteria:

- 최소 5-10 거래일에 대해 coverage report가 존재한다.
- target universe/session 기준 L1 coverage 60% pass 날짜가 여러 개 확보된다.
- point-in-time universe와 current universe 차이 리포트 1건을 확보한다.
- halt inference가 minute bar 기반으로 최소 1회 실행되고, 결과가 report에 연결된다.
- 2일 smoke replay와 5-10일 sanity replay가 `run_manifest.json`, KPI CSV, trade log, bucket diff, execution quality를 모두 남긴다.
- 1개월 결과는 검증이 아니라 calibration 후보 축소 용도로만 문서화한다.

PR/커밋 단위:

1. `data: strengthen point-in-time coverage reporting`
2. `data: add universe diff report for replay dates`
3. `data: wire halt inference into coverage workflow`
4. `report: summarize 5-10 day sanity replay`
5. `docs: update Step 0 exit status and calibration rules`

검증 명령:

```bash
./scripts/psradar report-backtest-coverage --market-date YYYY-MM-DD --session premarket
./scripts/psradar report-backtest-coverage --market-date YYYY-MM-DD --session regular --infer-halts
./scripts/psradar run-paper-trading
./scripts/psradar review-paper-performance --export-dir sample_outputs/paper_trading
./scripts/check_quality.sh
```

`--strict-coverage-gate`는 Step 0 60%가 안정적으로 나온 뒤에만 켠다.

```bash
./scripts/check_quality.sh --strict-coverage-gate
```

### v3 - 장기 범위

목표: 실매매 판단이 가능한 out-of-sample + shadow 검증으로 넘어간다.

Exit criteria:

- 튜닝 구간과 검증 구간이 분리된다.
- 3개월 이상 out-of-sample replay를 고정 파라미터로 1회 실행한다.
- 2-4주 shadow 결과가 저장되고, backtest KPI와 편차 리포트를 생성한다.
- Step 4 현실화 체결 모델이 적용되어 수정 전보다 수익률이 낮아지는지 확인한다.
- Step 5 KPI가 A/B/baseline 비교, one-winner dependency, stop slippage, cost-adjusted return을 포함한다.
- live readiness 문서로 넘어갈지 말지가 명확히 결정된다.

검증 명령:

```bash
./scripts/check_quality.sh --strict-coverage-gate
./scripts/psradar run-premkt-predictor
./scripts/psradar run-paper-trading
./scripts/psradar review-paper-performance --export-dir sample_outputs/paper_trading
./scripts/psradar automation-status --format json
```

### 문서 갱신 순서

1. `docs/STATUS.md`: 현재 우선순위와 최신 coverage/gate 상태.
2. `docs/STEP_PROGRESS_KO.md`: active step, 완료 기준, 검증 명령.
3. `docs/BACKTEST_ROADMAP_KO.md`: exit criteria 변경이나 Step 범위 조정.
4. `docs/OPERATIONS_KO.md`: 실제 반복 실행 명령이 안정화된 뒤.
5. `README.md`: 새 top-level 명령이나 진입점이 생길 때만.

### 지금 하지 말아야 할 것

- live trading 전환.
- full tape/websocket 실시간 엔진.
- 여러 data provider 확장.
- Step 0 coverage 전 파라미터 튜닝.
- bucket 컬럼 대규모 rename/DB migration.
- 대시보드 polish.
- 의도 없는 golden snapshot 변경.
- 1개월 결과를 edge 검증으로 주장하기.
- coverage 60% 안정화 전 CI hard fail 강제.

### 완벽한 설계 vs 지금 가장 가치 있는 설계

| 구분 | 완벽한 설계 | 지금 가장 가치 있는 설계 |
| --- | --- | --- |
| 데이터 | 전 종목 historical L1/tape 확보 | 선택 universe/date/session에 대해 60% gate를 통과하는 archive 루프 |
| bucket | bucket 용어 전면 리네임 | `bucket`/`strategy_bucket` 의미를 테스트와 CSV 계약으로 고정 |
| halt | 모든 halt/LULD 원천 데이터 통합 | minute gap 기반 halt inference로 replay 해석 위험 축소 |
| 검증 | 자동 walk-forward/grid optimizer | 2일 smoke, 5-10일 sanity, 1개월 calibration, 3개월 OOS를 수동으로라도 재현 가능하게 만들기 |
